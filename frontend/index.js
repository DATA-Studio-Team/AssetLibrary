/*const xScrollElement = document.getElementById("x-sroll");

xScrollElement.addEventListener("wheel", (evt) => {
    evt.preventDefault();
    xScrollElement.scrollBy(evt.deltaY, 0)
});*/

//document.dispatchEvent(new KeyboardEvent('keydown', {'key': 'Shift'}))